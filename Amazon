creating new pipeline for jenkins
also we need to automate the pipeline for continous building
Set<String> set = people.stream()
   .map(Person::getName)
   .collect(Collectors.toCollection(TreeSet::new));
creating new pipeline for jenkins
also we need to automate the pipeline for continous building
Set<String> set = people.stream()
   .map(Person::getName)
   .collect(Collectors.toCollection(TreeSet::new));
